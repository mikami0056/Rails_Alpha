<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
</p>

<table>
  <thead>
  <tr>
    <th>Comment</th>
  </tr>
  </thead>

  <tbody>
  <% @posts.each do |post| %>
  <tr>
    <td><%= post.content %></td>
  </tr>
  <% end %>
  </tbody>
</table>